# 结算功能升级 - 产品设计文档

---

## 文档信息

| 项目名称 | 民匠有约管理系统 - 结算功能升级 |
|---------|----------------|
| 文档版本 | V1.0 |
| 创建日期 | 2026-08-02 |
| 更新日期 | 2026-08-03 |
| 负责人 | 产品部 |

---

## 修订记录

| 版本 | 日期 | 修订内容 |
|-----|------|---------|
| V1.0 | 2026-08-02 | 初始版本，包含企业端任务详情预结算/确认结算功能设计 |
| V1.1 | 2026-08-02 | 新增小程序端结算功能设计（首页、任务列表、结算记录、个人中心） |
| V1.2 | 2026-08-02 | 结算记录整合为统一列表，提现记录去除备注字段 |
| V1.3 | 2026-08-03 | 确认结算独立为标签页，预结算上传逻辑优化，灵工确认四态支持 |
| V1.4 | 2026-08-03 | 精简文档，移除企业端其他页面更新和安心云页面更新章节 |

---

## 一、升级背景

原结算功能存在以下问题：
1. 企业端任务详情页缺少完整的预结算→确认结算流程
2. 结算操作分散，用户体验不流畅
3. 缺少灵工确认环节，无法保障结算合规性
4. 小程序端缺少结算相关页面
5. 结算记录分散在多个 Tab 中，查看不便

本次升级旨在：
- 建立完整的「预结算 → 灵工确认 → 确认结算」流程闭环
- 优化操作体验，减少跳转和弹窗
- 支持灵工确认四态管理
- 补齐小程序端结算功能

---

## 二、企业端任务详情页

### 2.1 页面结构

任务详情页采用标签页布局，包含三个并列标签：

| 标签页 | 说明 |
|-------|------|
| 人员列表 | 管理任务参与人员，发起预结算 |
| 打卡保险 | 查看打卡记录与保险信息 |
| 确认结算 | 对预结算人员进行最终结算确认 |

### 2.2 人员列表

#### 2.2.1 搜索功能
- 支持按姓名搜索
- 支持按手机号搜索
- 支持清空搜索条件

#### 2.2.2 批量操作
| 操作按钮 | 说明 |
|---------|------|
| 预结算 | 勾选人员后发起预结算，弹出预结算窗口 |
| 添加成员 | 添加新的任务参与人员 |
| 二维码邀请 | 生成邀请二维码供人员扫码加入 |
| 导出打卡 | 导出人员打卡记录 |
| 导出保险 | 导出人员保险信息 |

> **注意**：原"确认结算"按钮已移除，改为预结算提交后自动跳转。

#### 2.2.3 列表字段

| 字段 | 说明 |
|-----|------|
| 姓名 | 人员姓名 |
| 手机号 | 联系电话 |
| 身份证号 | 身份证号码 |
| 工种 | 人员工种 |
| 加入时间 | 加入任务时间 |
| 成员状态 | 已加入/已挂起 |
| 应结算 | 预期结算金额 |
| 预结算 | 预结算金额（蓝色高亮显示） |
| 实结金额 | 实际结算金额（绿色显示） |
| 结算时间 | 最终结算时间 |
| 灵工确认 | 待确认/已确认/已拒绝/无需确认 |
| 备注 | 备注信息 |
| 操作 | 挂起、到场确认 |

> **注意**：原操作列中的"结算"按钮已移除，结算统一通过确认结算标签页处理。

### 2.3 预结算

#### 2.3.1 预结算窗口

点击"预结算"按钮后弹出预结算窗口，展示勾选的人员列表。

**窗口内容**：
- 搜索框：支持按姓名搜索
- 灵工确认开关：开启后提交预结算将发送结算单给灵工确认
- 预结算列表表格：姓名、身份证号、预结算金额（可编辑）、备注（可编辑）
- 统计信息：人数、预结算总额
- 下载模版按钮、上传Excel按钮

#### 2.3.2 Excel 批量导入

**下载模版**：
- 导出当前预结算列表为 Excel 模版
- 包含字段：姓名、身份证号、预结算金额、备注

**上传 Excel**：
- 支持 .xlsx、.xls 格式
- 解析 Excel 数据并更新预结算金额和备注

**选中人员上传逻辑（V1.3 新增）**：
- 如果在预结算窗口中勾选了特定人员，上传 Excel 时**只更新勾选人员的数据**
- 如果未勾选任何人员，上传 Excel 时更新预结算列表中所有人员的数据
- 提示信息区分："已更新选中的 N 条" vs "已更新 N 条"

#### 2.3.3 提交预结算

**处理逻辑**：
1. 将预结算窗口中的数据同步到主人员列表
2. 如果开启了"灵工确认"开关，将人员状态设为"待确认"
3. 关闭预结算窗口
4. **自动跳转到"确认结算"标签页**（V1.3 新增）
5. 自动构建确认结算列表，只展示预结算过的人员（preSettleAmount > 0）

### 2.4 确认结算

#### 2.4.1 数据来源

确认结算列表**仅展示经过预结算提交的人员**，过滤条件：
- 成员状态为"已加入"
- 预结算金额大于 0

#### 2.4.2 搜索与筛选

| 筛选项 | 说明 |
|-------|------|
| 姓名 | 按姓名搜索 |
| 灵工确认 | 待确认/已确认/已拒绝/无需确认 |

#### 2.4.3 统计卡片

| 统计项 | 颜色 | 说明 |
|-------|------|------|
| 结算人数 | 默认 | 当前筛选后总人数 |
| 已确认 | 绿色 #00b578 | 灵工已确认的人数 |
| 待确认 | 橙色 #ff9500 | 等待灵工确认的人数 |
| 已拒绝 | 红色 #ff4d4f | 灵工拒绝的人数 |
| 无需确认 | 灰色 #86909c | 无需灵工确认的人数 |
| 预结算总额 | 默认 | 筛选后人员预结算金额总和 |
| 支付总计 | 默认 | 筛选后人员支付金额总和 |

#### 2.4.4 灵工确认四态

| 状态 | 说明 | 可结算 | 颜色 |
|-----|------|-------|------|
| 待确认 | 需等待灵工确认结算单 | 否 | 橙色（warning） |
| 已确认 | 灵工已确认 | 是 | 绿色（success） |
| 已拒绝 | 灵工拒绝 | 否 | 红色（danger） |
| 无需确认 | 无需灵工确认 | 是 | 灰色（info） |

**状态切换逻辑**（点击状态标签循环切换）：
```
待确认 → 已确认 → 已拒绝 → 无需确认 → 待确认
空值/无需确认 → 待确认
```

#### 2.4.5 列表字段

| 字段 | 说明 |
|-----|------|
| 选择框 | 仅"已确认"和"无需确认"状态可选 |
| 姓名 | 人员姓名 |
| 身份证号 | 身份证号码 |
| 预结算金额 | 预结算金额 |
| 备注 | 备注信息 |
| 灵工确认 | 当前确认状态（Tag 标签显示） |

#### 2.4.6 结算提交

**操作流程**：
1. 勾选需要结算的人员（仅已确认/无需确认可选）
2. 输入手机号
3. 点击"获取验证码"（60秒倒计时）
4. 输入验证码
5. 点击"提交结算"

**校验规则**：
- 至少选择一条记录
- 必须输入验证码
- 选中人员中不能有"待确认"或"已拒绝"状态的人员

**提交成功后**：
- 更新人员实结金额
- 更新结算时间
- 灵工确认状态清空
- 自动切回"人员列表"标签页

---

## 三、小程序端结算功能

### 3.1 小程序首页

展示用户的核心信息概览：
- 任务统计卡片
- 结算收入概览
- 快捷入口

### 3.2 任务列表

展示用户参与的任务列表：
- 任务卡片展示任务名称、状态、金额等信息
- **结算单确认弹窗**：在任务卡片上点击"确认结算单"按钮，弹出结算单确认弹窗，展示预结算金额并供灵工确认

### 3.3 结算记录

#### 3.3.1 统一列表（V1.2 更新）

原结算记录页面采用 Tab 切换（结算/提现），现已整合为统一列表展示：

**展示内容**：
- 交易类型：结算/提现
- 金额
- 时间
- 状态

#### 3.3.2 提现记录优化（V1.2 更新）

- 提现记录不再显示备注字段
- 与结算记录整合在同一列表中，通过类型区分

### 3.4 个人中心

展示用户个人信息：
- 头像、昵称、手机号
- 实名认证状态
- 银行卡管理
- 设置入口

---

## 四、结算流程总览

### 4.1 完整流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        企业端操作流程                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 人员列表                    2. 预结算                        │
│  ┌──────────┐                 ┌──────────────────┐              │
│  │ 勾选人员  │ ────点击────→  │ 弹出预结算窗口    │              │
│  │          │                 │                  │              │
│  │ 搜索筛选  │                 │ - 调整金额       │              │
│  │ 添加成员  │                 │ - Excel批量导入  │              │
│  │ 导出打卡  │                 │   (仅更新选中)   │              │
│  │ 导出保险  │                 │ - 灵工确认开关   │              │
│  └──────────┘                 └────────┬─────────┘              │
│                                        │                        │
│                                        │ 提交                    │
│                                        ↓                        │
│  3. 确认结算（自动跳转）                                        │
│  ┌──────────────────────────────────────────────────┐           │
│  │                                                    │           │
│  │  仅展示预结算过的人员                               │           │
│  │                                                    │           │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐           │           │
│  │  │ 统计卡片 │  │ 搜索筛选 │  │ 列表表格 │           │           │
│  │  └─────────┘  └─────────┘  └────┬────┘           │           │
│  │                                 │                  │           │
│  │  灵工确认状态：                  │                  │           │
│  │  待确认 → 已确认 → 已拒绝 → 无需确认              │           │
│  │                                 │                  │           │
│  │                          勾选+验证码               │           │
│  │                                 │                  │           │
│  └─────────────────────────────────┼──────────────────┘           │
│                                    ↓                              │
│  4. 提交结算 → 更新实结金额/结算时间 → 返回人员列表               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        小程序端操作流程                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 任务列表 → 查看任务卡片 → 点击"确认结算单"                    │
│  2. 弹出结算单确认弹窗 → 查看预结算金额 → 确认/拒绝               │
│  3. 结算记录 → 查看统一列表（结算+提现）                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 状态流转图

```
                    灵工确认开关
                    ┌────────┐
                    │ 开启   │
                    │   ↓    │
  预结算提交 ──────→ 待确认 ──→ 已确认 ──→ 可结算 ──→ 提交结算 ──→ 完成
                    │              ↑
                    │              │
                    │         灵工拒绝
                    │              │
                    └──────────→ 已拒绝（不可结算）

                    ┌────────┐
                    │ 关闭   │
                    │   ↓    │
  预结算提交 ──────→ 无需确认 ──→ 可结算 ──→ 提交结算 ──→ 完成
```

---

## 五、技术实现要点

### 5.1 技术栈

| 技术 | 版本 | 用途 |
|-----|------|------|
| Vue 3 | 3.x | 前端框架 |
| Element Plus | 2.x | UI 组件库 |
| Vite | 5.x | 构建工具 |
| XLSX | 0.18 | Excel 文件解析 |
| Vue Router | 4.x | 路由管理 |

### 5.2 关键实现

#### 5.2.1 标签页切换

使用 `v-show` + `activeTab` 状态控制标签页切换，避免组件重复渲染：

```javascript
const tabs = [
  { key: 'members', label: '人员列表' },
  { key: 'attendance', label: '打卡保险' },
  { key: 'confirm-settle', label: '确认结算' }
]
const activeTab = ref('members')
```

#### 5.2.2 预结算上传选中人员逻辑

```javascript
const hasSelection = preSettleSelected.value.length > 0
const targets = hasSelection ? preSettleSelected.value : preSettleList.value
// 仅在 targets 范围内查找匹配项并更新
```

#### 5.2.3 确认结算列表构建

```javascript
const buildConfirmSettleList = () => {
  confirmSettleList.value = memberList.value
    .filter(m => m.memberStatus === '已加入' && Number(m.preSettleAmount || 0) > 0)
    .map(m => ({ ...m }))
}
```

#### 5.2.4 灵工确认四态

| 状态值 | 可结算 | selectable |
|-------|-------|-----------|
| 待确认 | 否 | false |
| 已确认 | 是 | true |
| 已拒绝 | 否 | false |
| 无需确认 | 是 | true |

---

## 六、涉及文件清单

### 6.1 企业端

| 文件 | 说明 |
|-----|------|
| [TaskDetail.vue](file:///workspace/src/views/Company/TaskDetail.vue) | 任务详情页（核心修改） |

### 6.2 小程序端

| 文件 | 说明 |
|-----|------|
| [MiniLayout.vue](file:///workspace/src/layout/MiniLayout.vue) | 小程序布局 |
| [Home.vue](file:///workspace/src/views/Mini/Home.vue) | 小程序首页 |
| [TaskList.vue](file:///workspace/src/views/Mini/TaskList.vue) | 任务列表（含结算单确认弹窗） |
| [Settlement.vue](file:///workspace/src/views/Mini/Settlement.vue) | 结算记录（统一列表） |
| [Profile.vue](file:///workspace/src/views/Mini/Profile.vue) | 个人中心 |

### 6.3 配置文件

| 文件 | 说明 |
|-----|------|
| [router/index.js](file:///workspace/src/router/index.js) | 路由配置 |
| [vite.config.js](file:///workspace/vite.config.js) | Vite 配置 |

---

*文档版本：V1.4*
*更新日期：2026-08-03*
